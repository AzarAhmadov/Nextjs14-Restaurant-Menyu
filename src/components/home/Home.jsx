import Hero from '../hero/Hero';
import Category from '../category/Category';

const Home = () => {

    return (
        <main>
            <Hero />
            <Category />
        </main>
    );
}

export default Home;
