/** @type {import('next').NextConfig} */
const nextConfig = {}
module.exports = {
  output: 'export',
  trailingSlash: true,
  images: {
    domains: ['localhost'],
  },
};
module.exports = nextConfig